# Anthropic API — Claude-разметка сегментов (corpus)
anthropic>=0.40.0

# Web-фреймворк + шаблоны
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.12

# PostgreSQL — асинхронный клиент для основной и аналитической БД
asyncpg>=0.30.0

# Планировщик задач (суточный запуск в 00:05)
apscheduler>=3.11.0

# Конфигурация из config.yml
pyyaml>=6.0.2
